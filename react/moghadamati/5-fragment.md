

اصلاً Fragment چیه؟

تو React هر کامپوننت باید فقط یک ریشه (root) المنت برگردونه.

یعنی این غلطه ❌

return (
  <h1>Hello</h1>
  <p>World</p>
);

چون دو تا المنت هم‌سطح داری.


---

راه‌حل مبتدی‌ها 😅

میان همه‌چی رو می‌ذارن داخل یه div:

return (
  <div>
    <h1>Hello</h1>
    <p>World</p>
  </div>
);

ولی مشکلش چیه؟
اون div اضافه میره تو DOM و بعضی وقتا layout رو خراب می‌کنه 👀


---

اینجا Fragment وارد میشه 💎

Fragment اجازه میده چندتا المنت برگردونی بدون اینکه یه نود اضافه تو DOM ساخته شه.

روش اول (کامل):

import React from "react";

function Example() {
  return (
    <React.Fragment>
      <h1>Hello</h1>
      <p>World</p>
    </React.Fragment>
  );
}


---

روش دوم (خفن‌تر و رایج‌تر):

function Example() {
  return (
    <>
      <h1>Hello</h1>
      <p>World</p>
    </>
  );
}

اون <> </> همون Fragment ـه فقط کوتاه‌نویسی شده.


---

یه مثال واقعی‌تر (حس پروژه‌ای 🔥)

فرض کن داری لیست کارت‌ها رو render می‌کنی:

function Cards() {
  return (
    <>
      <Header />
      <Card />
      <Card />
      <Footer />
    </>
  );
}

اینجا اگه div میذاشتی، یه wrapper اضافی میرفت تو DOM.
Fragment اینو حل می‌کنه.


---

یه نکته مهم حرفه‌ای 🧠

اگر داخل map از Fragment استفاده می‌کنی و نیاز به key داری،
نمی‌تونی از <> </> استفاده کنی. باید از نسخه کامل استفاده کنی:

items.map(item => (
  <React.Fragment key={item.id}>
    <h2>{item.title}</h2>
    <p>{item.desc}</p>
  </React.Fragment>
));


---

جمع‌بندی سریع

Fragment یعنی:

> چند تا المنت برگردون، بدون div اضافی تو 